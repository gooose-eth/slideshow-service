import { instance } from './index.js'
import { ERROR_CODE } from '../libs/assets.js'

/**
 * model admin
 *
 * @param {number} page
 * @return {Promise<object>}
 */
export async function modelAdminItems({ page })
{
  let result = {
    total: 0,
    items: [],
  }
  // TODO: 슬라이드 목록
}

// TODO: 슬라이드 만들기
// TODO: 슬라이드 수정
// TODO: 슬라이드 삭제
